Range = range(1, 10)

num = int(input("Enter one single number between the range 1 to 10"))

if num in Range:

    print("Number is in the range")

else:

    print("Number is not in the range")